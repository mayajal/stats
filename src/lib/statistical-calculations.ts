
import type { DataRow, VariableMapping, AnalysisResult, AnalysisType } from '@/types';

// This file is now empty as the fetch logic has been moved to a Next.js Server Action in src/app/actions.ts
// The function `performAnalysisAction` now handles the backend communication.
// This was done to resolve persistent client-side CORS and fetch errors.
// Keeping the file for now to avoid breaking imports, but it can be removed in a future cleanup.
